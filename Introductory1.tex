\documentclass[11pt]{article}

\begin{document}
	Hello! This id First in \LaTeX\ document
	% hard Return = Give a space
	
	We left space up for a new line.\\ We can also do a soft return by using 2 slashed. But line is not indented. 
	
	To enter math mode $(x^2+3x+4)$. And to protect the equation to be splited in 2 lines use ${A(x) = 3x^3+7x^2+9}$ 
	To use displayed math Mode $$3x^2 + 3x+1$$ and to use inline math mode $3x^2 + 3x+1$
	
\end{document}